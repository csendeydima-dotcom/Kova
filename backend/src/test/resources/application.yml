spring:
  datasource:
    url: jdbc:h2:mem:kova;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
kova:
  session-days: 30
  cookie-secure: false
  resend-api-key:
  email-from: Kova <test@example.com>
  google-client-id:
  verification-pepper: test-pepper
